import { MapListConditionPipe } from './map-list-condition.pipe';

describe('MapListConditionPipe', () => {
  it('create an instance', () => {
    const pipe = new MapListConditionPipe();
    expect(pipe).toBeTruthy();
  });
});
