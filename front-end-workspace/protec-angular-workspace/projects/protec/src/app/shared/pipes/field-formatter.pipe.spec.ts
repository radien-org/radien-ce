import { FieldFormatterPipe } from './field-formatter.pipe';

describe('FieldFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
