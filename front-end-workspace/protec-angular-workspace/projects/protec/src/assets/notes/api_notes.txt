generic:

- getText(lang)

cookie banner:

- setCookiBanner(radio, button) response(cookie list{ABC,CD})

- setCookie(send info from cookie list)

insurance:

- getInsuranceList() response(insurance_list) 

obs: se nada foi escolhido envia o não na primeira, ou seja o não ja começa selecionado
se o user selecionar as opções mostrar os outros btns.

Nessa segunda opção o não tbm vem selecionado

work accident and private accident:

obs: caso seja selecionado a terceira opção deve ir normalmente para private mas apos ele deve ir para tela da 
terceira opção. Apos esse terceira pula o body.

- getOptions() response(optionList)

private accident:

obs: ver a regra de esconder a segunda data

part body:

- getOption(partBody) response(optionList)

- getSubOption(partBody, options[]) response(optionList)

summary:

obs: se estiver logado
saveDataAcquisition(allData{}) response(true ou false)

getDataAcquisition() response(accidentList[])

editAccidents(allData{accidentId:0, allData})

deleteAccident(accidentId) response(true or false)

chanceOfSuccess:

getStatus() response(0,1,2)

personalData:

getUserPersonalData() response(allData)

personalDataContact:

obs: não rpecisa de get por que a informação vem no get da personal data

obs: salvar os dados apos essa tela.

getAddress(zipcode) response(Address)

createUserPersonalDataAndContact(allData) response(true or false)

resume user data update:

updateUserPersonalDataAndContact(allData) response(true or false)

chance of succes model:

sendEmail() response(true or false)







