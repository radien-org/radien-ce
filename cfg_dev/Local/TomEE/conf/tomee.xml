<?xml version="1.0" encoding="UTF-8"?>
<tomee>
	<!-- see http://tomee.apache.org/containers-and-resources.html -->
	<Resource id="jdbc/AppFrameDS" type="javax.sql.DataSource">
		accessToUnderlyingConnectionAllowed = ${accessToUnderlyingConnectionAllowed}
		alternateUsernameAllowed = ${alternateUsernameAllowed}
		connectionProperties =
		defaultAutoCommit = ${defaultAutoCommit}
		defaultReadOnly =
		definition =
		ignoreDefaultValues = ${ignoreDefaultValues}
		initialSize = ${initialSize}
		jdbcDriver = ${HSQL_jdbcDriver}
		jdbcUrl = ${HSQL_jdbcUrl}
		jtaManaged = ${jtaManaged}
		maxActive = ${maxActive}
		maxIdle = ${maxIdle}
		maxOpenPreparedStatements = ${maxOpenPreparedStatements}
		maxWaitTime = ${maxWaitTime}
		minEvictableIdleTime = ${minEvictableIdleTime}
		minIdle = ${minIdle}
		numTestsPerEvictionRun = ${numTestsPerEvictionRun}
		password =
		passwordCipher = ${passwordCipher}
		poolPreparedStatements = ${poolPreparedStatements}
		serviceId =
		testOnBorrow = ${testOnBorrow}
		testOnReturn = ${testOnReturn}
		testWhileIdle = ${testWhileIdle}
		timeBetweenEvictionRuns = ${timeBetweenEvictionRuns}
		userName = ${HSQL_userName}
		validationQuery = ${HSQL_validationQuery}
	</Resource>
</tomee>
